<%= link_to new_article_path do %>
  <p><i class="fas fa-plus-circle" style="margin: 10px;"></i>Add a new task</p>
<% end %>

  <% @articles.each do |article| %>
   <div class="container">
      <h3><%= link_to article.title, article_path(article) %></h3>
      <p><%= article.content %></p>
    </div>
  <% end %>

